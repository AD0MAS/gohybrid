/** Type styling shared by "Add block" (WorkoutBuilder) and "Add item"
 * (BlockEditor), so the two labels can't drift apart in colour or weight. */
export const ADD_BUTTON_TEXT_CLASSES = "text-[13px] font-medium text-ink";
